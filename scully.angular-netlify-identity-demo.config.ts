import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "angular-netlify-identity-demo",
  outDir: './dist/static',
  routes: {
  }
};